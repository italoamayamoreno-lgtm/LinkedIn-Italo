name: Generate LinkedIn Posts

on:
  workflow_dispatch:
    inputs:
      brief:
        description: 'Brief en JSON'
        required: true

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - name: Generate posts via Claude
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          echo "Generando posts..."
